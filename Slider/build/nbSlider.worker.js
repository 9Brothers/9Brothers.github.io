'use strict';
function BoaNoite(e) {
    postMessage("Boa noite " + e.name + " " + e.surname);
}
function BomDia(e) {
    postMessage("Bom dia " + e.name + " " + e.surname);
}
function BoaTarde(e) {
    postMessage("Boa tarde " + e.name + " " + e.surname);
}
